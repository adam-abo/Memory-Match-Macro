import pyautogui as pag
import time

keys = ['a','d','a','m']
key = 'adam'

inter = 0
pag.write(key)
time.sleep(inter)

#adamabc
    
####################################################################################################################
'''
    type = 'Regular'
    pag.press('e')
    time.sleep(3)
    pag.press('\\')
    pag.press('a')
    pag.press('a')
    pag.press('s')

    if type:
        games[type] += 1
        items = memory_match.play(type)
        collected = testing.identify(items, collected)
        print(collected)
    else:
        exit()

    for _ in range(8):
        time.sleep(15*60)
        pag.press('1')
    '''
###################################################################################################################